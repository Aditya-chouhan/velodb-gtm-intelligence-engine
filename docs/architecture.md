# Architecture

The MVP is deterministic by design: inputs → scoring → evidence validation → opportunity routing → strategy → personalization. Deterministic business logic provides auditability, predictable routing, easier experimentation, guardrails, and attribution. LLMs are best added for extraction, summarization, contradiction checks, and message synthesis.
